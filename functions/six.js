function multiplyNumbers (num1, num2, num3) {
  let result = num1 * num2 * num3;
  return result;
}

let product = multiplyNumbers(2, 3, 4);

/*
function arguments = 2, 3, 4
function body = let result = num1 * num2 * num3;
                 return result
function delclation = contents inside the curly brackets;
function name = multiplyNumbers
function parameters = num1, num2, num3
function return value = result
name of all the variables in this program = result, product, multiplyNumbers, num1, num2, num3
*/